<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:IT_Assets.Pages"
             x:Class="IT_Assets.Pages.EditAssetPage"
             Title="Edit Asset"
             BackgroundColor="White">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15" WidthRequest="450">

            <Label Text="Edit Asset" 
                FontAttributes="Bold" 
                FontSize="24" 
                HorizontalOptions="Center"
                TextColor="#0066CC"/>

            <!-- Code (read-only) -->
            <HorizontalStackLayout>
                <Label Text="Code" TextColor="Gray" WidthRequest="120" VerticalOptions="Center"/>
                <Entry x:Name="CodeEntry" IsEnabled="False" BackgroundColor="#f0f0f0"  WidthRequest="300"/>
            </HorizontalStackLayout>
            
            <!-- Name -->
            <HorizontalStackLayout>
            <Label Text="Name" TextColor="Gray" WidthRequest="120" VerticalOptions="Center"/>
                <Entry x:Name="NameEntry" Placeholder="Enter asset name" WidthRequest="300"/>
            </HorizontalStackLayout>
            
            <!-- Model -->
            <HorizontalStackLayout>
                <Label Text="Model" TextColor="Gray" WidthRequest="120" VerticalOptions="Center"/>
                <Entry x:Name="ModelEntry" Placeholder="Enter model" WidthRequest="300"/>
            </HorizontalStackLayout>

            <!-- Location -->
            <HorizontalStackLayout>
            <Label Text="Location" TextColor="Gray" WidthRequest="120" VerticalOptions="Center"/>
                <Entry x:Name="LocationEntry" Placeholder="Enter location" WidthRequest="300"/>
            </HorizontalStackLayout>
            
             <!-- Maintenance Date -->
            <HorizontalStackLayout>
                <Label Text="Maintenance  Date" TextColor="Gray"  WidthRequest="119" VerticalOptions="Center"/>
                <DatePicker x:Name="MaintenanceDatePicker" WidthRequest="300"/>
             </HorizontalStackLayout>
                    <!-- Receipt Form -->
            <HorizontalStackLayout>
                <Label Text="Receipt Form" TextColor="Gray" WidthRequest="120" VerticalOptions="Center"/>
            <Entry x:Name="ReceiptFormEntry" Placeholder="Enter receipt form" WidthRequest="300"/>
            </HorizontalStackLayout>
                    <!-- Note -->
            <Label Text="Note" TextColor="Gray"/>
            <Editor x:Name="NoteEditor"
                Placeholder="Enter additional notes..."
                AutoSize="TextChanges"
                HeightRequest="100"
                BackgroundColor="#f9f9f9"/>

            <!-- Action Buttons -->
            <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                <Button Text="Save" 
                    Clicked="OnSaveClicked" 
                    BackgroundColor="#0066CC" 
                    TextColor="White"
                    CornerRadius="12"
                    WidthRequest="120"/>
                <Button Text="Cancel" 
                    Clicked="OnCancelClicked" 
                    BackgroundColor="Gray" 
                    TextColor="White"
                    CornerRadius="12"
                    WidthRequest="120"/>
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
    <!--   
<ContentPage.Behaviors>
    <BehaviorCollection>
        <EventToCommandBehavior EventName="OnSaveClicked" Command="{Binding SaveCommand}" />
    </BehaviorCollection>
</ContentPage.Behaviors>
-->
</ContentPage>
